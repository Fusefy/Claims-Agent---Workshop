# API Configuration
API_HOST=localhost
API_PORT=8000
RELOAD=True  # Set to False in production

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:5173

# ==========================================
# Gemini API Configuration
# ==========================================
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.0-flash

# ==========================================
# GCP Configuration (for Claims Processing)
# ==========================================
GCS_BUCKET_NAME=claimwise-claims
GCP_PROJECT_ID=your-gcp-project-id
GCP_CREDENTIALS_PATH=backend/gcp-credentials.json

# ==========================================
# Database Configuration
# ==========================================
# Local Development
DB_HOST=localhost
DB_PORT=5432
DB_NAME=claims_db
DB_USER=postgres
DB_PASSWORD=your_database_password

# Cloud SQL - PostgreSQL (Production)
# CLOUD_SQL_CONNECTION_NAME=your-project:us-central1:your-instance
# USE_PRIVATE_IP=true

# Database Pool Configuration
DB_POOL_MIN=5
DB_POOL_MAX=20

# ==========================================
# Setup Instructions:
# 1. Get GEMINI_API_KEY from: https://makersuite.google.com/app/apikey
# 2. Create GCS bucket in GCP Console (name: claimwise-claims)
# 3. Enable Cloud Vision API in your GCP project
# 4. Generate service account key with:
#    - Storage Admin role
#    - Cloud Vision API User role
# 5. Save the JSON key as backend/gcp-credentials.json
# 6. Update GCP_CREDENTIALS_PATH if using different location
# ==========================================